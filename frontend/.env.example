# GitHub OAuth Configuration
# Get your Client ID from: https://github.com/settings/developers
VITE_GITHUB_CLIENT_ID=your_github_oauth_client_id_here

# OAuth Proxy URL (Cloudflare Worker)
# Deploy oauth-worker first, then add the worker URL here
VITE_OAUTH_PROXY_URL=https://tangled-oauth-proxy.your-subdomain.workers.dev

# Repository Configuration
# This is the repository where data will be stored
VITE_GITHUB_REPO_OWNER=cinnamon-msft
VITE_GITHUB_REPO_NAME=tangled

# Branch Configuration (optional, defaults to 'main')
VITE_GITHUB_BRANCH=main
